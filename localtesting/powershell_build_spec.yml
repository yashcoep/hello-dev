version: 0.1
component: build
timeoutInSeconds: 6000
runAs: root
shell: powershell
env:
  variables:
    "testEnv" : "testValue1"
  exportedVariables:
    - testEnv

inputArtifacts:
  - name: hello-dev-jar
    location: /workspace/Source1/hello123.class

steps:
  - type: Command
    name: "Build Source"
    timeoutInSeconds: 4000
    command: |
      echo $testEnv
